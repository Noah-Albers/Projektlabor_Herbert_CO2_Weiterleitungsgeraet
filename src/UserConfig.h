#pragma once
#include <WiFi.h>

extern const char* WIFI_NAME;
extern const char* WIFI_PASSWORD;
extern const bool IP_USE_DHCP;
extern const IPAddress IP_STATIC_IP;
extern const IPAddress IP_STATIC_GATEWAY;
extern const IPAddress IP_STATIC_SUBNETMASK;
extern const IPAddress IP_STATIC_DNS;
extern const char* MASTER_SERVER;
extern const int DEVICE_ID;
extern const int KNOWN_DEVICES[];
extern const int KNOWN_DEVICES_AMOUNT;
